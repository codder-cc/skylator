# Core server dependencies (no inference backend — install one of the extras below)
fastapi>=0.111.0
uvicorn[standard]>=0.29.0
pydantic>=2.0.0
httpx>=0.27.0
pyyaml>=6.0
huggingface_hub>=0.22.0
zeroconf>=0.131.0   # optional: mDNS LAN discovery (safe to remove if not needed)
