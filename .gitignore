# ── Secrets / local config ────────────────────────────────────────────────────
config.yaml
server_config.yaml

# ── Python ────────────────────────────────────────────────────────────────────
__pycache__/
*.py[cod]
*.pyo
*.pyd
*.pyc
.Python

# ── Virtual environment ───────────────────────────────────────────────────────
venv/
.venv/
env/
ENV/

# ── Build / packaging ─────────────────────────────────────────────────────────
build/
dist/
*.egg-info/
.eggs/

# ── Runtime output ────────────────────────────────────────────────────────────
logs/
cache/
*.log

# ── Model files (GGUF weights — large binaries, never commit) ─────────────────
/models/
/remote_worker/models_cache/
/remote_worker/server_config.yaml
*.gguf
*.safetensors

# ── Translation working files ─────────────────────────────────────────────────
*.trans.json
translated_mods.txt
translation_progress.json

# ── IDEs ──────────────────────────────────────────────────────────────────────
.vscode/
.idea/
*.suo
*.user

# ── OS ────────────────────────────────────────────────────────────────────────
Thumbs.db
desktop.ini
.DS_Store

# ── Malformed pip install artefacts (e.g. "pip install =0.x.x") ─────────────
=*
