# ── Install torch FIRST with CUDA 12.8 (RTX 5080 Blackwell / sm_120) ──────────
# pip install torch torchvision --index-url https://download.pytorch.org/whl/cu128
# Then: pip install -r requirements.txt

# ── Core ML stack ─────────────────────────────────────────────────────────────
transformers>=4.49.0          # Qwen2.5 + GPTQ support
accelerate>=1.3.0             # device_map="auto", CPU offload
optimum[gptq]>=1.24.0         # GPTQ kernel integration
# auto-gptq removed — transformers>=4.49 loads GPTQ natively without it

# ── HuggingFace ecosystem ──────────────────────────────────────────────────────
huggingface_hub>=0.27.0       # snapshot_download for model caching
safetensors>=0.4.5            # fast .safetensors shard loading
sentencepiece>=0.2.0          # tokenizer (some models need it)

# ── Config and utilities ───────────────────────────────────────────────────────
pyyaml>=6.0.2
click>=8.1.8

# ── Context pipeline ───────────────────────────────────────────────────────────
requests>=2.32.0

# ── Web UI ─────────────────────────────────────────────────────────────────────
flask>=3.0.0              # Web framework for the UI

# ── macOS Apple Silicon backend (alternative to llama-cpp for Qwen3.5) ────────
# mlx-lm           # install via: pip install mlx-lm  (macOS only)

# ── Remote translation server (install when running server.py) ─────────────────
fastapi>=0.115.0          # High-performance async API framework for the translation server
uvicorn[standard]>=0.30.0 # ASGI server (httptools + websockets)
pydantic>=2.0.0           # Request/response models for FastAPI
zeroconf>=0.132.0         # mDNS service announcement + LAN discovery

# ── Dev / testing ──────────────────────────────────────────────────────────────
pytest>=8.3.0
